# Agents

## Testing

Write as much test coverage as realistically possible for any code changes, without going overboard. The goal is to prevent regression. When fixing bugs or adding features, write tests that cover the new/changed behavior, edge cases, and interactions with existing code.
